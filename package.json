{
  "name": "bitpanda-proxy",
  "version": "1.0.0",
  "description": "Proxy CORS per API Bitpanda",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  }
}
